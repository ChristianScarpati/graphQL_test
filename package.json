{
  "name": "graphql",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "grep -rnw ./ -e 'console.log' && echo 'Please remove console.logs before committing!' && exit 1 || exit 0"
    }
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.4.0",
    "graphql": "^16.6.0",
    "uuid": "^9.0.0"
    
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "description": ""
}
